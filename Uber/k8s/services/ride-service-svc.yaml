apiVersion: v1
kind: Service
metadata:
  name: ride-service
  namespace: uber
spec:
  selector:
    app: ride-service
  ports:
    - port: 8080
      targetPort: 8080
#vvv IF ANY ISSUE TRY REMOVING THIS vvv
  type: ClusterIP

