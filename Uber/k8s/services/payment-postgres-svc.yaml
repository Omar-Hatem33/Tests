# ClusterIP — used by payment-service to connect
apiVersion: v1
kind: Service
metadata:
  name: payment-postgres
  namespace: uber
spec:
  selector:
    app: payment-postgres
  ports:
    - port: 5432
      targetPort: 5432
