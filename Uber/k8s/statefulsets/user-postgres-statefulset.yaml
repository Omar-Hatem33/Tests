apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: user-postgres
  namespace: uber
spec:
  serviceName: user-postgres
  replicas: 1
  selector:
    matchLabels:
      app: user-postgres
  template:
    metadata:
      labels:
        app: user-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:17
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: user-postgres-secret
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: user-postgres-secret
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: user-postgres-secret
                  key: POSTGRES_DB
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - postgres
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - postgres
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            limits:
              memory: 512Mi
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        storageClassName: standard
        resources:
          requests:
            storage: 1Gi


#            TRY THIS IF  THERE WAS ISSUE


#apiVersion: apps/v1
#kind: StatefulSet
#metadata:
#  name: user-postgres
#  namespace: uber
#spec:
#  serviceName: user-postgres   # must match the headless Service name
#  replicas: 1
#  selector:
#    matchLabels:
#      app: user-postgres
#  template:
#    metadata:
#      labels:
#        app: user-postgres
#    spec:
#      containers:
#        - name: postgres
#          image: postgres:17
#          ports:
#            - containerPort: 5432
#          env:
#            - name: POSTGRES_USER
#              valueFrom:
#                secretKeyRef:
#                  name: user-postgres-secret
#                  key: POSTGRES_USER
#            - name: POSTGRES_PASSWORD
#              valueFrom:
#                secretKeyRef:
#                  name: user-postgres-secret
#                  key: POSTGRES_PASSWORD
#            - name: POSTGRES_DB
#              valueFrom:
#                secretKeyRef:
#                  name: user-postgres-secret
#                  key: POSTGRES_DB
#          resources:
#            limits:
#              memory: 512Mi
#            requests:
#              memory: 256Mi
#          # readinessProbe: K8s only sends traffic when pg_isready succeeds
#          # This is what makes "kubectl wait --for=condition=ready" meaningful
#          readinessProbe:
#            exec:
#              command: ["pg_isready", "-U", "postgres"]
#            initialDelaySeconds: 15
#            periodSeconds: 10
#            failureThreshold: 6
#          livenessProbe:
#            exec:
#              command: ["pg_isready", "-U", "postgres"]
#            initialDelaySeconds: 30
#            periodSeconds: 10
#            failureThreshold: 6
#          volumeMounts:
#            - name: data
#              mountPath: /var/lib/postgresql/data
#  volumeClaimTemplates:
#    - metadata:
#        name: data
#      spec:
#        accessModes: ["ReadWriteOnce"]
#        resources:
#          requests:
#            storage: 1Gi